#include <iostream>
#include <string>

using namespace std;

int main()
{
    int num_coins;
    if(!(cin >> num_coins) || num_coins <= 0){
        cout << "Invalid input" << endl;
        return 0;
    }

    
}